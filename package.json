{
  "name": "shortlinker",
  "version": "0.1.0",
  "private": true,
  "description": "Secure link shortener with analytics and expiry control",
  "scripts": {
    "vercel-build": "prisma generate && node prisma-migrate.js",
    "postinstall": "prisma generate && prisma migrate deploy"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "geoip-lite": "^1.4.10",
    "jsonwebtoken": "^9.0.2",
    "qrcode": "^1.5.4",
    "ua-parser-js": "^1.0.37",
    "validator": "^13.11.0"
  },
  "devDependencies": {
    "prisma": "^5.18.0"
  }
}